For now ill create a program that generates problems with just +, when that is done i will add another function so that the user can choose between operators and get problems with that operator. Might create a more difficult version later one with ** and roots etc
